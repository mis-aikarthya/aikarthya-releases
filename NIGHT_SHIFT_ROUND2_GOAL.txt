Follow NIGHT_SHIFT_ROUND2_AGENT_PROMPT.md exactly and complete ALL 14 Round 2 issues in Linear team AIK, project "M&E Console — Round 2 (Feedback Fixes)", AIK-28..41. Also obey NIGHT_SHIFT_LINEAR_WORKFLOW.md (shared-branch rule, per-issue lifecycle, hard rules, the Linear GraphQL status-transition fallback) and NIGHT_SHIFT_ROUND2_PLAN.md (stage spec + page/route map). Read all three before starting.

LOOP, do not stop after one issue: pick the lowest-numbered eligible issue (state Backlog or Todo with ALL its blockedBy issues Done — check actual Linear relations, never infer from titles), move it to In Progress, build on branch night-shift/me-mgmt-dashboard reusing the Round 1 widgets/providers the issue names, run flutter analyze until clean, verify via Codex (codex exec --sandbox workspace-write --ask-for-approval never; it runs the app on staging and judges every acceptance criterion incl. visual ones, returning a TEXT verdict) with Antigravity (agy) as the visual-review fallback, fix any FAIL yourself and re-verify, post the analyze output + Codex/agy text verdict as a Linear comment, commit (message "AIK-N: <what> — <why>" with the Co-Authored-By trailer), update the issue's Linear status, then immediately pick the next eligible issue. Newly-unblocked issues open up as their blockers reach Done (AIK-37/38/39 after AIK-28; AIK-40/41 after AIK-34). S0 (AIK-28/37/38: Android writes no location_pings while web works) is the priority — every location feature stays empty until it lands.

HARD RULES (never relax): staging only, NEVER push to prod, NEVER open a PR; secrets passed by PATH never by value (Linear key, Syncfusion key, staging login stay out of Linear/commits/logs/briefs); reuse before building. You have NO image recognition — never capture a screenshot and feed it to yourself; the visual PASS must arrive as TEXT from Codex/agy. No emojis anywhere. Residual-human items (AIK-28/37 physical-device ping test, AIK-33 Syncfusion license key, AIK-36 last-login data source) get built + AI-verified as far as possible, flagged needs-review with the exact residual, and the loop CONTINUES — they do not stall the run.

DONE — clear this goal only when the transcript shows ALL 14 issues (AIK-28..41) reported at their final Linear status: Done for fully-verifiable issues, or In Review/needs-review for the residual-human items, EACH with its flutter analyze result and its Codex (or agy) text verdict, and the night-shift/me-mgmt-dashboard branch committed and analyze-clean. End with a 14-row table: issue | final state | verdict | residual human step. If no eligible issue remains but some are not yet Done/needs-review, report why and continue. Or stop after 120 turns.
